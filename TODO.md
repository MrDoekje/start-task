- [ ] improved providers
  - [ ] split git/ticket providers up, there may be some overlap (e.g. github/gitlab can be either or ticket), the implementation would differ, so splitting them up should be OK
  - [ ] create default providers for many git providers
  - [ ] create default providers for many ticket providers
  - [ ] create many presets for different setup/steps/prompts

- [ ] make repo more claude independent

- [ ] for every flow, and per usage allow;
  - [ ] different git provider
  - [ ] different agent
  - [ ] support supplying useful argument per agents, perhaps build into config + arbitrary for easy compatibility

- [ ] ⁠create own layout wrapper instead of tmux
